function Array(arr){
    let count=0;
    let avg=0;
    let sum=0;
     for(let i=0;i<=arr.length-1;i++){
         sum=sum+arr[i];
         count++;
     }
     avg=sum/count;
     return avg;
}
console.log(Array([1,2,3,4,5]));
