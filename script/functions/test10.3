// calculating the cirle are with arguments and with return type

function carea(r){
    R=r
    area=(2*Math.PI)*R*R
    return area
}
r=parseInt(prompt("enter the radius of the circle: "))
console.log("the area of the circle is ",carea(r))
