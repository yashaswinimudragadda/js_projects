//calculating the fabnociss series with argument and with return
function fab(x){
    n=x
    i=1
    a=0
    b=1
     let series=[]
    while(i<=n){
        
        series.push(a)
        a=b
        c=a+b
        b=c
        i++
    }
    return series
}
x=parseInt(prompt("enter the value here"))
if(x>0){
    console.log(fab(x))
}
else{
    console.log("error:try again")
}
