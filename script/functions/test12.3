//calculating the perimeter of the circle with argument with return 
function perimeter(r){
    R=r;
    perimeter=Math.floor(2*(Math.PI)*r)
    return perimeter
}
console.log("the perimeter of the given radius is ",perimeter(1))
