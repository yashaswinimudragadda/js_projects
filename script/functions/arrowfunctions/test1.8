
// with arguments and with return

cyclic=(n=153)=>{
    f=0
    l=n%10
    while(n>0){
        f=n%10
        n=Math.floor(n/10)
    }
    result = (l===f)?"the given number is cyclic ":"the given number is not cyclic"
    return result
}
console.log(cyclic())

//with out arguments and with return type

cyclic=()=>{
    n=153
    l=n%10
    f=0
    while(n>0){
        f=n%10
        n=Math.floor(n/10)
    }
    result=(l===f)?"the given number is cyclic":"the given number is not cyclic"
    return result
}
console.log(cyclic())

//with arguments and without return 

cyclic=(n=153)=>{
    l=n%10
    f=0
    while(n>0){
        f=n%10
        n=Math.floor(n/10)
    }
    result=(l===f)?"the given number is cyclic":"the given number is not cyclic number"
    return result
}
console.log(cyclic())




// cyclic number

cyclic=()=>{
    n=121
    l=n%10
    f=0
    while(n>0){
        f=n%10
        n=Math.floor(n/10)
    }
    result=(l==f)?"the given number is cyclic":"the given number is not cyclic"
    console.log(result)
}
cyclic()
