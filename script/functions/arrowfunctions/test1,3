//calculation of simple instreset with arguments and with return

si=(r,t,p=100)=>{
    si=(p*r*t)/100
    return si
}
console.log("the simple interset is "+si(2,1))


//calculate the simple interset with out arguments with return

si=()=>{
    p=100
    r=2
    t=1
    si = (p*t*r)/100
    return si
}
console.log("the simple interset is "+si())





//calculating the si with argumetns and without return 

si=(p=100,r=2,t=1)=>{
    console.log("the simple interset is "+((p*r*t)/100))
}
si()





//calculating the simple instrest without arguments and without return

si=()=>{
    p=100
    r=2
    t=1
    console.log("the simple interset is "+((p*r*t)/100))
}
si()
