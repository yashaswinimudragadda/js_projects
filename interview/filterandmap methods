let users =[
    {id:1, name:"John"},
    {id:2, name:"Pete"},
    {id:3, name:"Mary"}
    ];
    
    let someUsers = users.filter(item => item.id<=3);
    
    alert(someUsers.length);
    
    console.log(someUsers.map(item => item.name));

//map: it call the function for each element in an array and return the array of result

let lengths = ["Bilbo","Gandalf","Nazgul"].map(item => item.length);
alert(lengths);

let names=["Bilbo","Gandalf","Nazgul"]
console.log(names.map(item=>item));
console.log(names.map(item => item ="Bilbo"));
console.log(names.map(item=> item === "Bilbo"));//true

// it also return the sorted array , but the returned alue is usually ignored as arr itself is modifies.


// the method arr.reverse the order of elements in arr.

let arr = [1,2,3,4,5];
arr.reverse();

alert(arr);//5,4,3,2,1

let arr =[1,2,15];

arr.sort();
alert(arr);// 1,15,2
// literally all elements are conerted to strings for comparisons. for strings, lexicographic ordering is applied and indeed "2">"15".
//To use our won sorting order we need to supply a function as the argument of arr.sort()


//the str.split(delim) method does exactly that . it splits the  string into an array by the given delimiter delim.

let names ='Bilbo,Ganadalf,Nazgul';
let arr = names.split(',');

for(let name of arr){
    alert(`A message to ${name}.`);
};

let arr1='Bilbo,Gandalf,Nazgul,Saruman'.split(', ',2);
alert(arr1);

let str="test";
alert(str.split(''));//t,e,s,t
