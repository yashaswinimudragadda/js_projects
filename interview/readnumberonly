function readNumber(){
    let num;
    
    
    do{
        num=prompt("enter your number here",0);
    }while(!isFinite(num));
    
    if(num === null || num === '') return null;
    
    return +num;
}

console.log(readNumber());
